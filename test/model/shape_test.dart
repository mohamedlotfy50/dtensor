import 'package:test/test.dart';

void main() {
  group('Shape class operations', () {
    // print(Shape.matMult(first, second)?.shape); //(9, 5, 7, 3)
// first = Shape(shape: [2, 2], size: 0, dimCount: []);
// second = Shape(shape: [2, 2], size: 0, dimCount: []);

// print(Shape.matMult(first, second)?.shape); //(2,2)

// first = Shape(shape: [2, 2], size: 0, dimCount: []);
// second = Shape(shape: [2], size: 0, dimCount: []);

// print(Shape.matMult(second, first)?.shape); //(2)
// print(Shape.matMult(first, second)?.shape); //(2)
  });
}
